# 🚀 Telegram Бот на базе Gemini

![Безымянный](https://github.com/user-attachments/assets/30c4f051-5249-45bd-933e-a8b671e871c4)

---

Данный проект представляет собой Telegram-бота, использующего API Google Gemini для генерации ответов и работы с контекстом в режиме памяти.  
Бот написан на **Python** с использованием библиотеки **pyTelegramBotAPI** и предназначен для развертывания через **Docker**.


#### 📣 Если вы из РФ, вам требуется запускать бота на зарубежном VPS либо подключать к боту VPN или VLESS прокси. Из РФ запросы к Gemini блокируются!
---


## ⚙️ Основной функционал

- **Интеграция с Gemini**  
  Получение ответов от моделей Google Gemini, с настройкой имени модели через административные команды.

- **Управление ролями и именем**  
  Пользователи могут задавать пользовательскую системную роль и имя для бота, которые будут использоваться в их диалогах.

- **Контекст диалога**  
  Поддержка истории общения, ограниченной параметром `MAX_HISTORY_MESSAGES` (по умолчанию **5**).

- **Форматирование HTML**  
  Ответы ИИ форматируются с использованием HTML. Бот автоматически очищает ответы от недопустимых тегов, разрешая только:  
  - `<b>` — **жирный**  
  - `<i>` — *курсив*  
  - `<s>` — ~~зачеркнутый~~  
  - `<code>` — `инлайн-код`  
  - `<pre>` — блок кода

- **Масштабируемость**  
  Длинные ответы (>3000 символов) автоматически разбиваются на части с сохранением форматирования.

- **Контроль доступа**  
  Поддерживается два режима:  
  - `Free_Chat = 1` — свободный доступ для всех  
  - `Free_Chat = 0` — доступ только по белому списку `ALLOWED_USER_IDS`

- **Dockerized**  
  Упакован в Docker-контейнер для лёгкого и надёжного развертывания.

---

## 🛠️ Настройка и развертывание

### 1. Переменные окружения (`.env`)

Создайте файл `.env`, основываясь на `.env.example`, и заполните обязательные параметры:

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `TG_BOT_TOKEN` | Ключ Telegram-бота | `КОД_БОТА_БОТФАЗЕР` |
| `ADMIN_ID` | ID администратора | `123456789` |
| `ALLOWED_USER_IDS` | Список ID (через запятую) для режима `Free_Chat = 0` | `"123456789,123456"` |
| `Free_Chat` | Режим доступа: `1` — всем, `0` — только из списка | `1` |
| `MAX_HISTORY_MESSAGES` | Максимальное количество сообщений в контексте | `5` |

##### Остальные настройки внесите от админа бота в самом боте (команда бота `/kb`)


---

### 2. Зависимости

Проект требует следующих Python-пакетов:

```txt
requests
pyTelegramBotAPI
python-dotenv
```

---

### 3. Развертывание с помощью Docker

Используйте docker-compose для сборки и запуска бота:
```
bashdocker-compose build
docker-compose up -d
```
Убедитесь, что файлы `docker-compose.yml` и `Dockerfile` находятся в корне проекта.

---
- ##### 💬 Команды
Бот поддерживает как текстовые команды, так и интерактивную клавиатуру.
- ##### 🌐 Общие команды (доступны всем)

```
/role_save — Установить пользовательское имя и роль
/role_load — Показать текущую роль и имя
/role_reset — Сбросить пользовательскую роль
/reset — Очистить историю диалога (контекст)
/kb — Показать/обновить основную клавиатуру
```

При вызове `/role_save` бот запросит сначала имя, затем роль (системный промпт).


- ##### 👮 Административные команды (только для ADMIN_ID)
```
/key_set — Установить/обновить GEMINI_API_KEY
/set_name_ai_neyro — Сменить модель Gemini (например, gemini-1.5-flash-latest)
/set_name_bot — Изменить имя бота по умолчанию
/kb_admin — Показать админскую клавиатуру
```

- ##### ✅ Готов к использованию в продакшене!
Просто соберите, настройте .env и запустите через Docker — и бот будет доступен 24/7.
